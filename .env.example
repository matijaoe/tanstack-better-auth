# Copy this file to .env.local
# Values are generated by `npx convex dev` on first run

CONVEX_DEPLOYMENT=dev:your-deployment-name
VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# -----------------------------------------------
# Convex server-side env vars (set via CLI, not in this file)
#
# Required:
#   npx convex env set BETTER_AUTH_SECRET "$(openssl rand -base64 32)"
#   npx convex env set SITE_URL http://localhost:3000
#
# Required for production (add --prod):
#   npx convex env set RP_ID your-domain.com --prod
#
# See SETUP.md for full deployment instructions.
# -----------------------------------------------
