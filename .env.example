# ===========================================
# Local env vars (copy this file to .env.local)
# ===========================================

# Convex deployment identifier (found in Convex dashboard or after running `npx convex dev`)
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Convex cloud URL (ends in .convex.cloud)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex HTTP actions URL (ends in .convex.site)
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# ===========================================
# Convex env vars (set via Convex CLI, NOT in this file)
#
#   npx convex env set BETTER_AUTH_SECRET "$(openssl rand -base64 32)"
#   npx convex env set SITE_URL http://localhost:3000
#
# Optionally, for production passkey config:
#   npx convex env set RP_ID yourdomain.com
# ===========================================
